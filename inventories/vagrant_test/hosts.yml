# Local Vagrant/QEMU test environment — see test/vagrant/README.md.
# Not a real client; used to run cpu_patch_precheck.yml/cpu_patch_apply.yml
# against an actual 19c CDB before touching any client site.

all:
  children:
    oracle_db_hosts:
      hosts:
        vagrant-ora19c:
          ansible_host: 127.0.0.1
          ansible_port: 2222
          oracle_home: /u01/app/oracle/product/19.0.0/dbhome_1
          grid_home: ""
