---
# Changelog:
#   2026-09-15T23:04:48+05:30 — Initial version — sample per-patch variable file — Arvind Regukumar
#   2026-09-16T00:24:55+05:30 — Removed "(with Claude Code)" suffix from the author field per project convention — Arvind Regukumar
# Sample patch run definition. Copy to vars/patches/<PATCH_ID>.yml per
# quarterly CPU/RU. No secrets here — connections use OS-authenticated
# `/ as sysdba` (local bequeath on the DB host), so there's no password to
# vault for the common case. See README.md if a client site has OS
# authentication disabled.

patch_id: "34765931"
patch_description: "19.24 Release Update (Oct 2024 CPU/RU)"
patch_zip_local_path: "/Users/youruser/patches/p34765931_190000_Linux-x86-64.zip"
patch_staging_dir: /u01/patches
target_hosts: oracle_db_hosts

cdbs:
  - sid: CDB1
    pdbs: [PDB1, PDB2]

# Never fill this in here — pass at apply time:
#   -e confirm_patch=34765931
confirm_patch: ""
